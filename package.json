{
  "name": "brokenlink-ai",
  "private": true,
  "version": "0.1.0",
  "description": "Full-stack SaaS starter that scans websites for broken links",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "build": "npm run build --workspace client",
    "postinstall": "npm run prisma:generate --workspace server || (cd server && npx prisma generate)",
    "prisma:generate": "cd server && npx prisma generate && echo '✅ Prisma client generated successfully'",
    "start": "npm run start --workspace server"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
